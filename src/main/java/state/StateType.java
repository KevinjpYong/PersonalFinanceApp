package state;

/**
 * Enum for all the possible state
 * **/
public enum StateType {
    SELECTION_STATE,
    SAVING_STATE,
    LIABILITY_STATE,
    STATEMENT_STATE,
    GOAL_STATE,
    BUDGETING_STATE
}
